const computer = {
    brand: 'lenovo',
    price: 45000,
    processor: 'inter',
    ssd: '512gb'
}

const keys  = Object.keys(computer)
const values  = Object.values(computer)
console.log(values);
console.log(keys);